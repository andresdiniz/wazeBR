<div class="container-fluid px-4 py-3">
        <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h4 class="mb-1 fw-bold text-dark">
                <i class="fas fa-bell me-2 text-warning"></i>Configurações de Notificação
            </h4>
            <p class="text-muted mb-0 small">Gerencie seus canais de notificação e alertas personalizados</p>
        </div>
        <button class="btn btn-warning btn-lg shadow-sm" data-bs-toggle="modal" data-bs-target="#notificationModal" id="addNotificationBtn">
            <i class="fas fa-plus me-2"></i>Nova Notificação
        </button>
    </div>

        <div class="card border-0 shadow-sm">
        <div class="card-body p-0">
                        <div class="row g-3 p-4 pb-3 bg-light bg-opacity-50">
                {% set stats = [
                    {'icon': 'fas fa-bell', 'color': 'secondary', 'title': 'Total de Configurações', 'count': notification_settings|length if notification_settings else 0},
                    {'icon': 'fas fa-toggle-on', 'color': 'success', 'title': 'Ativas', 'count': (notification_settings|selectattr('enabled')|list|length) if notification_settings else 0},
                    {'icon': 'fab fa-whatsapp', 'color': 'success', 'title': 'WhatsApp', 'count': (notification_settings|selectattr('type', 'equalto', 'whatsapp')|list|length) if notification_settings else 0},
                    {'icon': 'fas fa-envelope', 'color': 'primary', 'title': 'E-mail', 'count': (notification_settings|selectattr('type', 'equalto', 'email')|list|length) if notification_settings else 0}
                ] %}

                {% for stat in stats %}
                <div class="col-md-3">
                    <div class="d-flex align-items-center p-3 bg-white rounded shadow-sm">
                        <div class="flex-shrink-0">
                            <i class="{{ stat.icon }} text-{{ stat.color }} fs-4"></i>
                        </div>
                        <div class="flex-grow-1 ms-3">
                            <div class="fw-bold text-dark">{{ stat.count }}</div>
                            <small class="text-muted">{{ stat.title }}</small>
                        </div>
                    </div>
                </div>
                {% endfor %}
            </div>

                        <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead class="table-light">
                        <tr>
                            <th class="text-center fw-semibold" style="width: 60px;">
                                <i class="fas fa-hashtag text-muted"></i>
                            </th>
                            <th class="fw-semibold">
                                <i class="fas fa-broadcast-tower me-2 text-muted"></i>Canal
                            </th>
                            <th class="fw-semibold">
                                <i class="fas fa-info-circle me-2 text-muted"></i>Descrição
                            </th>
                            <th class="fw-semibold">
                                <i class="fas fa-map-marker-alt me-2 text-muted"></i>Destino
                            </th>
                            <th class="text-center fw-semibold">
                                <i class="fas fa-power-off me-2 text-muted"></i>Status
                            </th>
                            <th class="text-center fw-semibold" style="width: 140px;">
                                <i class="fas fa-cogs me-2 text-muted"></i>Ações
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for notif in notification_settings %}
                        <tr class="border-bottom">
                            <td class="text-center">
                                <span class="badge bg-light text-dark border">{{ notif.id }}</span>
                            </td>
                            <td>
                                <div class="d-flex align-items-center">
                                    {% if notif.type == 'whatsapp' %}
                                        <div class="bg-success bg-opacity-10 rounded-circle p-2 me-3">
                                            <i class="fab fa-whatsapp text-success"></i>
                                        </div>
                                    {% else %}
                                        <div class="bg-primary bg-opacity-10 rounded-circle p-2 me-3">
                                            <i class="fas fa-envelope text-primary"></i>
                                        </div>
                                    {% endif %}
                                    <span class="fw-medium">{{ notif.type|capitalize }}</span>
                                </div>
                            </td>
                            <td>
                                <span class="text-dark">{{ notif.description }}</span>
                            </td>
                            <td>
                                <code class="bg-light text-dark p-1 rounded small">{{ notif.destination }}</code>
                            </td>
                            <td class="text-center">
                                {% if notif.enabled %}
                                    <span class="badge bg-success-subtle text-success border border-success-subtle px-3 py-2">
                                        <i class="fas fa-check-circle me-1"></i>Ativo
                                    </span>
                                {% else %}
                                    <span class="badge bg-secondary-subtle text-secondary border border-secondary-subtle px-3 py-2">
                                        <i class="fas fa-pause-circle me-1"></i>Inativo
                                    </span>
                                {% endif %}
                            </td>
                            <td class="text-center">
                                <div class="btn-group" role="group">
                                    <button class="btn btn-outline-primary btn-sm btn-edit-notification"
                                        data-bs-toggle="modal"
                                        data-bs-target="#notificationModal"
                                        data-all='{{ notif|json_encode }}'
                                        data-action="edit"
                                        data-bs-toggle="tooltip"
                                        title="Editar notificação">
                                        <i class="fas fa-edit"></i>
                                    </button>
                                    <button class="btn btn-outline-secondary btn-sm"
                                        data-bs-toggle="tooltip"
                                        title="Testar notificação">
                                        <i class="fas fa-paper-plane"></i>
                                    </button>
                                    <button class="btn btn-outline-danger btn-sm"
                                        data-bs-toggle="tooltip"
                                        title="Excluir notificação">
                                        <i class="fas fa-trash-alt"></i>
                                    </button>
                                </div>
                            </td>
                        </tr>
                        {% else %}
                        <tr>
                            <td colspan="6" class="text-center py-5">
                                <div class="text-muted">
                                    <i class="fas fa-bell-slash fs-1 mb-3 opacity-25"></i>
                                    <h5 class="mb-2">Nenhuma configuração encontrada</h5>
                                    <p class="mb-3">Comece criando sua primeira notificação personalizada</p>
                                    <button class="btn btn-warning btn-sm" data-bs-toggle="modal" data-bs-target="#notificationModal">
                                        <i class="fas fa-plus me-2"></i>Criar primeira notificação
                                    </button>
                                </div>
                            </td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="notificationModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-xl modal-dialog-centered">
        <div class="modal-content border-0 shadow">
            <div class="modal-header bg-gradient text-white" style="background: linear-gradient(135deg, #ffc107 0%, #ff8f00 100%);">
                <div>
                    <h5 class="modal-title mb-1">
                        <i class="fas fa-cog me-2"></i>Configurar Notificação
                    </h5>
                    <small class="opacity-75">Configure os alertas e canais de comunicação</small>
                </div>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>

            <form method="POST" id="notificationForm">
                <input type="hidden" name="form_type" value="configure_notification">
                <input type="hidden" name="id" id="notif_id">
                
                <div class="modal-body p-0">
                                        <div class="bg-light bg-opacity-50 p-4 border-bottom">
                        <h6 class="fw-bold mb-3">
                            <i class="fas fa-sliders-h me-2 text-warning"></i>Configurações Básicas
                        </h6>
                        <div class="row g-3">
                            <div class="col-md-6">
                                <label for="notif_type" class="form-label fw-medium">
                                    <i class="fas fa-broadcast-tower me-1"></i>Canal de Notificação
                                </label>
                                <select class="form-select form-select-lg" name="type" id="notif_type" required>
                                    <option value="">Selecione um canal...</option>
                                    <option value="email">
                                        📧 E-mail
                                    </option>
                                    <option value="whatsapp">
                                        📱 WhatsApp
                                    </option>
                                </select>
                            </div>
                            <div class="col-md-6">
                                <label for="notif_destination" class="form-label fw-medium">
                                    <i class="fas fa-map-marker-alt me-1"></i>Destino
                                </label>
                                <input type="text" class="form-control form-control-lg" name="destination" id="notif_destination" 
                                    placeholder="Ex: email@exemplo.com ou +5511999999999" required>
                                <div class="form-text">
                                    <small><i class="fas fa-info-circle me-1"></i>Para WhatsApp, inclua o código do país</small>
                                </div>
                            </div>
                            <div class="col-md-8">
                                <label for="notif_description" class="form-label fw-medium">
                                    <i class="fas fa-tag me-1"></i>Descrição
                                </label>
                                <input type="text" class="form-control form-control-lg" name="description" id="notif_description" 
                                    placeholder="Ex: Alertas de Trânsito - Centro" maxlength="100" required>
                            </div>
                            <div class="col-md-4 d-flex align-items-end">
                                <div class="form-check form-switch form-check-lg">
                                    <input class="form-check-input" type="checkbox" id="notif_enabled" name="enabled">
                                    <label class="form-check-label fw-medium" for="notif_enabled">
                                        <i class="fas fa-power-off me-1"></i>Ativar
                                    </label>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                                        <div class="p-4">
                        <div class="d-flex justify-content-between align-items-center mb-3">
                            <h6 class="fw-bold mb-0">
                                <i class="fas fa-bell me-2 text-warning"></i>Tipos de Alertas
                            </h6>
                            <div class="btn-group btn-group-sm" role="group">
                                <button type="button" class="btn btn-outline-secondary" id="selectAllAlerts">
                                    <i class="fas fa-check-square me-1"></i>Marcar Todos
                                </button>
                                <button type="button" class="btn btn-outline-secondary" id="unselectAllAlerts">
                                    <i class="fas fa-square me-1"></i>Desmarcar Todos
                                </button>
                            </div>
                        </div>
                        
                        <div class="accordion accordion-flush" id="alertsAccordion">
                            {% set alert_groups = [
                                {
                                    'id': 'ACCIDENT', 
                                    'icon': 'fas fa-car-crash text-danger', 
                                    'title': 'Acidentes', 
                                    'subtitle': '3 tipos disponíveis',
                                    'badge_class': 'bg-danger-subtle text-danger',
                                    'alerts': [
                                        {'value': 'ACCIDENT_ACCIDENT_MINOR', 'label': 'Acidente Menor'},
                                        {'value': 'ACCIDENT_ACCIDENT_MAJOR', 'label': 'Acidente Grave'},
                                        {'value': 'ACCIDENT_NO_SUBTYPE', 'label': 'Acidente Geral'}
                                    ]
                                },
                                {
                                    'id': 'JAM', 
                                    'icon': 'fas fa-traffic-light text-warning', 
                                    'title': 'Congestionamentos', 
                                    'subtitle': '5 tipos disponíveis',
                                    'badge_class': 'bg-warning-subtle text-warning',
                                    'alerts': [
                                        {'value': 'JAM_JAM_MODERATE_TRAFFIC', 'label': 'Tráfego Moderado'},
                                        {'value': 'JAM_JAM_HEAVY_TRAFFIC', 'label': 'Tráfego Pesado'},
                                        {'value': 'JAM_JAM_STAND_STILL_TRAFFIC', 'label': 'Tráfego Parado'},
                                        {'value': 'JAM_JAM_LIGHT_TRAFFIC', 'label': 'Tráfego Leve'},
                                        {'value': 'JAM_NO_SUBTYPE', 'label': 'Congestionamento Geral'}
                                    ]
                                },
                                {
                                    'id': 'WEATHERHAZARD', 
                                    'icon': 'fas fa-exclamation-triangle text-warning', 
                                    'title': 'Perigos e Hazards', 
                                    'subtitle': 'Perigos na via, clima e acostamento',
                                    'badge_class': 'bg-warning-subtle text-warning',
                                    'alerts': [
                                        {'value': 'WEATHERHAZARD_HAZARD_ON_ROAD', 'label': 'Perigo na Via'},
                                        {'value': 'WEATHERHAZARD_HAZARD_ON_SHOULDER', 'label': 'Perigo no Acostamento'},
                                        {'value': 'WEATHERHAZARD_HAZARD_WEATHER', 'label': 'Perigo Climático'},
                                        {'value': 'WEATHERHAZARD_HAZARD_ON_ROAD_OBJECT', 'label': 'Objeto na Via'}
                                    ]
                                }
                            ] %}

                            {% for group in alert_groups %}
                            <div class="accordion-item border rounded mb-2">
                                <h2 class="accordion-header" id="heading{{ group.id }}">
                                    <button class="accordion-button collapsed py-3" type="button" data-bs-toggle="collapse" 
                                        data-bs-target="#collapse{{ group.id }}" aria-expanded="false" aria-controls="collapse{{ group.id }}">
                                        <div class="d-flex align-items-center w-100">
                                            <i class="{{ group.icon }} me-3 fs-5"></i>
                                            <div class="flex-grow-1">
                                                <div class="fw-bold">{{ group.title }}</div>
                                                <small class="text-muted">{{ group.subtitle }}</small>
                                            </div>
                                            <span class="badge {{ group.badge_class }} me-2">{{ group.alerts|length }}</span>
                                        </div>
                                    </button>
                                </h2>
                                <div id="collapse{{ group.id }}" class="accordion-collapse collapse" 
                                    aria-labelledby="heading{{ group.id }}" data-bs-parent="#alertsAccordion">
                                    <div class="accordion-body bg-light bg-opacity-25">
                                        <div class="row g-2">
                                            {% for alert in group.alerts %}
                                            <div class="col-md-6">
                                                <div class="form-check p-2 border rounded bg-white">
                                                    <input class="form-check-input alert-checkbox" type="checkbox" 
                                                        name="alerts[]" value="{{ alert.value }}" 
                                                        id="alert_cb_{{ alert.value|replace(' ', '_')|upper }}">
                                                    <label class="form-check-label fw-medium" for="alert_cb_{{ alert.value|replace(' ', '_')|upper }}">
                                                        {{ alert.label }}
                                                    </label>
                                                </div>
                                            </div>
                                            {% endfor %}
                                        </div>
                                    </div>
                                </div>
                            </div>
                            {% endfor %}
                        </div>
                    </div>
                </div>

                                <div class="modal-footer p-3 bg-light d-flex justify-content-between">
                    <div>
                        <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">
                            Cancelar
                        </button>
                    </div>
                    <div>
                        <button type="submit" class="btn btn-warning" id="saveNotificationBtn">
                            <i class="fas fa-save me-2"></i>Salvar Configuração
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

<script>
$(document).ready(function () {
    const notificationModal = $('#notificationModal');
    const notificationForm = $('#notificationForm');
    const notifId = $('#notif_id');
    const notifType = $('#notif_type');
    const notifDescription = $('#notif_description');
    const notifDestination = $('#notif_destination');
    const notifEnabled = $('#notif_enabled');
    const modalTitle = notificationModal.find('.modal-title');
    const saveBtn = $('#saveBtn');
    const alertCheckboxes = $('.alert-checkbox');

    // Inicializar tooltips
    var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'));
    var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
        return new bootstrap.Tooltip(tooltipTriggerEl);
    });

    // Função para resetar o formulário
    function resetForm() {
        notificationForm[0].reset();
        notifId.val('');
        notifType.prop('disabled', false);
        modalTitle.html('<i class="fas fa-plus me-2"></i>Nova Notificação');
        saveBtn.html('<i class="fas fa-plus me-2"></i>Criar Notificação');
        alertCheckboxes.prop('checked', false);
        
        // Fechar todos os acordeões
        $('.accordion-collapse').removeClass('show');
    }

    // Marcar/Desmarcar todos os alertas
    $('#selectAllAlerts').on('click', function() {
        alertCheckboxes.prop('checked', true);
        $(this).addClass('btn-success').removeClass('btn-outline-secondary');
        setTimeout(() => {
            $(this).removeClass('btn-success').addClass('btn-outline-secondary');
        }, 1000);
    });

    $('#unselectAllAlerts').on('click', function() {
        alertCheckboxes.prop('checked', false);
        $(this).addClass('btn-danger').removeClass('btn-outline-secondary');
        setTimeout(() => {
            $(this).removeClass('btn-danger').addClass('btn-outline-secondary');
        }, 1000);
    });

    // Configura o modal para edição
    $('.btn-edit-notification').on('click', function () {
        const data = $(this).data('all');
        notifId.val(data.id);
        notifType.val(data.type).prop('disabled', true);
        notifDescription.val(data.description);
        notifDestination.val(data.destination);
        notifEnabled.prop('checked', data.enabled == 1);
        modalTitle.html('<i class="fas fa-edit me-2"></i>Editar Notificação');
        saveBtn.html('<i class="fas fa-save me-2"></i>Salvar Alterações');

        // Preenche os checkboxes com base nos alertas salvos
        alertCheckboxes.prop('checked', false);
        if (data.alerts && data.alerts.length > 0) {
            data.alerts.forEach(alertValue => {
                $(`input[name="alerts[]"][value="${alertValue}"]`).prop('checked', true);
            });
        }
    });

    // Configura o modal para adicionar nova notificação
    $('#addNotificationBtn, .btn:contains("Criar primeira notificação")').on('click', function () {
        resetForm();
    });

    // Resetar o formulário ao fechar o modal
    notificationModal.on('hidden.bs.modal', function () {
        resetForm();
    });

    // Feedback visual no formulário
    notificationForm.on('submit', function(e) {
        saveBtn.html('<i class="fas fa-spinner fa-spin me-2"></i>Salvando...');
        saveBtn.prop('disabled', true);
    });

    // Validação dinâmica do destino baseado no tipo
    notifType.on('change', function() {
        const type = $(this).val();
        const destinationInput = $('#notif_destination');
        
        if (type === 'email') {
            destinationInput.attr('type', 'email');
            destinationInput.attr('placeholder', 'Ex: usuario@exemplo.com');
        } else if (type === 'whatsapp') {
            destinationInput.attr('type', 'tel');
            destinationInput.attr('placeholder', 'Ex: +5511999999999');
        }
    });
});
</script>