<body class="bg-gray-100" id="page-top">

    <div id="wrapper" class="flex h-screen">

        <div id="sidebar-menu"
            class="bg-gray-800 w-64 flex-shrink-0 sidebar-transition lg:block"
            aria-expanded="false">

            <a class="flex items-center justify-center p-4 h-16 bg-gray-900 shadow-md" href="/home">
                <div class="text-white text-xl font-bold tracking-wider flex items-center">
                    <img src="{{ settings.logo }}" alt="{{ settings.systemname }}" class="h-8 w-8 mr-3">
                    <span class="hidden lg:inline">{{ settings.systemname }}</span>
                </div>
            </a>

            <hr class="border-gray-700 my-0">

            <nav class="flex flex-col p-4 space-y-2">

                <a class="nav-link-item" href="dashboard">
                    <i class="fas fa-fw fa-tachometer-alt mr-3"></i>
                    <span>Dashboard</span>
                </a>

                <hr class="border-gray-700 my-2">

                <div class="text-xs font-semibold text-gray-400 uppercase tracking-wider mt-4 mb-2">
                    Dados ao Vivo
                </div>

                <a class="nav-link-collapse" href="#collapseTwo" id="collapse-link-1">
                    <i class="fas fa-fw fa-map-marker-alt mr-3"></i>
                    <span>Monitoramento</span>
                    <i class="fas fa-angle-right ml-auto text-sm transition-transform duration-300"></i>
                </a>
                <div class="space-y-1 pl-4 hidden" id="collapseTwo">
                    <a class="nav-link-item-child" href="routes">
                        <span>Rotas</span>
                    </a>
                    <a class="nav-link-item-child" href="irregularidades">
                        <span>Irregularidades</span>
                    </a>
                </div>

                {# Adicione aqui seus outros itens de menu conforme a necessidade #}

            </nav>
            
            <div class="p-4 text-center hidden lg:block">
                 <button id="sidebarToggle" class="bg-gray-700 hover:bg-gray-600 text-white p-2 rounded-full transition duration-150 ease-in-out">
                    <i class="fas fa-angle-left"></i>
                </button>
            </div>

        </div>
        <div id="content-wrapper" class="flex-1 flex flex-col overflow-y-auto">

            <nav class="bg-white shadow-md h-16 flex items-center justify-between px-6 sticky top-0 z-10">
                <div class="flex items-center space-x-4">
                     <button id="mobile-menu-toggle" class="lg:hidden text-gray-500 hover:text-gray-700">
                        <i class="fas fa-bars text-xl"></i>
                    </button>
                    <h1 class="text-xl font-semibold text-gray-800">{{ pagedata.title }}</h1>
                </div>

                <ul class="flex items-center space-x-4">
                    <li class="hidden md:block">
                        <div class="relative">
                            <input type="text" placeholder="Buscar..." class="p-2 border border-gray-300 rounded-lg focus:ring-blue-500 focus:border-blue-500">
                            <button class="absolute right-0 top-0 mt-2 mr-3 text-gray-400">
                                <i class="fas fa-search"></i>
                            </button>
                        </div>
                    </li>

                    <li class="relative">
                        <a id="userDropdown" class="flex items-center text-gray-700 hover:text-gray-900 cursor-pointer" aria-haspopup="true">
                            <span class="mr-2 hidden lg:inline text-sm font-medium">
                                {{ current_user.name|default('Usuário') }}
                            </span>
                            <img class="h-10 w-10 rounded-full object-cover" src="{{ current_user.avatar|default('img/default-avatar.png') }}" alt="Avatar">
                        </a>
                        <div id="userDropdownMenu" class="absolute right-0 mt-2 w-48 bg-white rounded-md shadow-xl z-20 hidden">
                            <a href="profile.html" class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100">
                                <i class="fas fa-user fa-sm fa-fw mr-2 text-gray-400"></i>
                                Perfil
                            </a>
                            <div class="border-t border-gray-100"></div>
                            <a href="#" data-modal-target="logoutModal" class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100">
                                <i class="fas fa-sign-out-alt fa-sm fa-fw mr-2 text-gray-400"></i>
                                Logout
                            </a>
                        </div>
                    </li>

                </ul>
            </nav>
            <main class="p-6 flex-grow">
                ```

**Estilos de Utilitário (Adicione no seu `<style>` ou importe em um arquivo CSS separado):**
```css
/* Estilos para o menu lateral */
.nav-link-item {
    @apply flex items-center p-3 text-sm font-medium text-gray-300 rounded-lg hover:bg-gray-700 transition duration-150;
}
.nav-link-collapse {
    @apply flex items-center p-3 text-sm font-medium text-gray-300 rounded-lg hover:bg-gray-700 transition duration-150 cursor-pointer;
}
.nav-link-item-child {
    @apply block p-2 text-sm text-gray-400 hover:text-white hover:bg-gray-700 rounded-md transition duration-150;
}