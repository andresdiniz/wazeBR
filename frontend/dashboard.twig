{% block stylesheets %}
<link rel="stylesheet" href="https://cdn.datatables.net/v/bs5/dt-1.13.10/r-2.5.0/datatables.min.css">
<style>
    :root {
        --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        --success-gradient: linear-gradient(135deg, #1cc88a 0%, #13855c 100%);
        --warning-gradient: linear-gradient(135deg, #f6c23e 0%, #dda20a 100%);
        --danger-gradient: linear-gradient(135deg, #e74a3b 0%, #be2617 100%);
        --info-gradient: linear-gradient(135deg, #36b9cc 0%, #258391 100%);
        --card-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
        --card-shadow-hover: 0 8px 30px rgba(0, 0, 0, 0.12);
        
        /* Variáveis para modo claro */
        --bg-color: #f8f9fc;
        --card-bg: #ffffff;
        --text-primary: #2c3e50;
        --text-secondary: #858796;
        --text-muted: #858796;
        --border-color: #e3e6f0;
        --table-hover: #f8f9fc;
        --header-border: #e3e6f0;
        --empty-icon-color: #d1d3e2;
    }

    /* Modo Escuro */
    body.dark-mode {
        --bg-color: #1a1d29;
        --card-bg: #252836;
        --text-primary: #e4e6eb;
        --text-secondary: #b0b3b8;
        --text-muted: #8a8d93;
        --border-color: #3a3d4a;
        --table-hover: #2d3142;
        --header-border: #3a3d4a;
        --empty-icon-color: #3a3d4a;
        --card-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
        --card-shadow-hover: 0 8px 30px rgba(0, 0, 0, 0.4);
    }

    /* Header da página */
    .page-header {
        margin-bottom: 2rem;
        padding-bottom: 1rem;
        border-bottom: 2px solid var(--header-border);
    }

    .page-header h1 {
        font-size: 2rem;
        font-weight: 700;
        color: var(--text-primary);
        margin-bottom: 0.5rem;
    }

    .page-header .subtitle {
        color: var(--text-secondary);
        font-size: 0.95rem;
    }

    /* Cards de estatísticas */
    .stats-card {
        background: var(--card-bg);
        border-radius: 1rem;
        padding: 1.5rem;
        box-shadow: var(--card-shadow);
        transition: all 0.3s ease;
        border-left: 4px solid transparent;
        height: 100%;
    }

    .stats-card:hover {
        transform: translateY(-5px);
        box-shadow: var(--card-shadow-hover);
    }

    .stats-card.primary {
        border-left-color: #667eea;
    }

    .stats-card.success {
        border-left-color: #1cc88a;
    }

    .stats-card.info {
        border-left-color: #36b9cc;
    }

    .stats-card.warning {
        border-left-color: #f6c23e;
    }

    .stats-card-icon {
        width: 60px;
        height: 60px;
        border-radius: 1rem;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.8rem;
        margin-bottom: 1rem;
    }

    .stats-card.primary .stats-card-icon {
        background: linear-gradient(135deg, rgba(102, 126, 234, 0.1), rgba(118, 75, 162, 0.1));
        color: #667eea;
    }

    .stats-card.success .stats-card-icon {
        background: linear-gradient(135deg, rgba(28, 200, 138, 0.1), rgba(19, 133, 92, 0.1));
        color: #1cc88a;
    }

    .stats-card.info .stats-card-icon {
        background: linear-gradient(135deg, rgba(54, 185, 204, 0.1), rgba(37, 131, 145, 0.1));
        color: #36b9cc;
    }

    .stats-card.warning .stats-card-icon {
        background: linear-gradient(135deg, rgba(246, 194, 62, 0.1), rgba(221, 162, 10, 0.1));
        color: #f6c23e;
    }

    .stats-card-value {
        font-size: 2.5rem;
        font-weight: 800;
        line-height: 1;
        margin-bottom: 0.5rem;
        color: var(--text-primary);
    }

    .stats-card-label {
        font-size: 0.9rem;
        color: var(--text-secondary);
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    /* Cards modernos */
    .modern-card {
        border: none;
        border-radius: 1rem;
        overflow: hidden;
        transition: all 0.3s ease;
        box-shadow: var(--card-shadow);
        background: var(--card-bg);
        height: 100%;
    }

    .modern-card:hover {
        box-shadow: var(--card-shadow-hover);
    }

    .modern-card-header {
        background: var(--primary-gradient);
        color: white;
        padding: 1.25rem 1.5rem;
        font-weight: 700;
        display: flex;
        align-items: center;
        justify-content: space-between;
        font-size: 1.05rem;
    }
    
    /* Novo estilo para o Modal Header */
    .modal-header.bg-primary-gradient {
        background: var(--primary-gradient) !important;
        border-bottom: none;
        border-radius: 1rem 1rem 0 0;
        padding: 1.25rem 1.5rem;
    }

    .modern-card-header i {
        font-size: 1.2rem;
    }

    .modern-card-header.bg-danger-gradient {
        background: var(--danger-gradient);
    }

    .modern-card-header.bg-warning-gradient {
        background: var(--warning-gradient);
    }

    .modern-card-header.bg-dark-gradient {
        background: linear-gradient(135deg, #5a6268 0%, #343a40 100%);
    }

    .modern-card-header.bg-info-gradient {
        background: var(--info-gradient);
    }

    /* Badge contador */
    .badge-counter {
        background: rgba(255, 255, 255, 0.25);
        padding: 0.4rem 0.75rem;
        border-radius: 50px;
        font-weight: 700;
        font-size: 0.95rem;
        backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.3);
    }

    /* Tabelas melhoradas */
    .modern-table {
        margin-bottom: 0;
    }

    .modern-table thead th {
        background: var(--table-hover);
        border: none;
        font-weight: 700;
        text-transform: uppercase;
        font-size: 0.75rem;
        color: var(--text-secondary);
        padding: 1rem 0.75rem;
        letter-spacing: 0.5px;
    }

    .modern-table tbody tr {
        transition: all 0.2s ease;
        border-bottom: 1px solid var(--border-color);
    }

    .modern-table tbody tr:hover {
        background-color: var(--table-hover);
        transform: translateX(3px);
    }

    .modern-table tbody tr:last-child {
        border-bottom: none;
    }

    .modern-table tbody td {
        padding: 1rem 0.75rem;
        vertical-align: middle;
        color: var(--text-primary);
    }

    .modern-table .fw-bold {
        color: var(--text-primary);
        font-weight: 700;
    }

    .modern-table .text-muted,
    .modern-table small.text-muted {
        color: var(--text-muted) !important;
    }

    /* Badges melhorados */
    .modern-badge {
        padding: 0.5rem 0.875rem;
        border-radius: 50px;
        font-weight: 700;
        font-size: 0.8rem;
        min-width: 65px;
        display: inline-block;
        text-align: center;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }

    /* Botões de ação */
    .action-btn {
        width: 36px;
        height: 36px;
        border-radius: 50%;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        transition: all 0.3s ease;
        border: none;
        margin: 0 3px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }

    .action-btn:hover {
        transform: translateY(-3px) scale(1.1);
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
    }

    .action-btn i {
        font-size: 0.9rem;
    }

    /* Estado vazio */
    .empty-state {
        text-align: center;
        padding: 4rem 1rem;
    }

    .empty-state i {
        font-size: 5rem;
        color: var(--empty-icon-color);
        margin-bottom: 1.5rem;
        opacity: 0.4;
    }

    .empty-state h5 {
        color: var(--text-primary);
        font-weight: 600;
        margin-bottom: 0.5rem;
    }

    .empty-state p {
        color: var(--text-secondary);
        font-size: 0.95rem;
        margin-bottom: 0;
    }

    /* Animações */
    .fade-in {
        animation: fadeIn 0.6s ease-out;
    }

    @keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(30px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    /* Botão de exportar */
    .export-btn {
        border-radius: 0.5rem;
        padding: 0.625rem 1.5rem;
        font-weight: 600;
        box-shadow: var(--card-shadow);
        transition: all 0.3s ease;
    }

    .export-btn:hover {
        transform: translateY(-2px);
        box-shadow: var(--card-shadow-hover);
    }

    /* Responsividade */
    @media (max-width: 767.98px) {
        .page-header h1 {
            font-size: 1.5rem;
        }

        .stats-card-value {
            font-size: 2rem;
        }

        .stats-card-icon {
            width: 50px;
            height: 50px;
            font-size: 1.5rem;
        }

        .modern-table {
            font-size: 0.85rem;
        }

        .modern-table thead th,
        .modern-table tbody td {
            padding: 0.75rem 0.5rem;
        }

        .action-btn {
            width: 32px;
            height: 32px;
        }
    }

    /* DataTables no modo escuro */
    body.dark-mode .dataTables_wrapper .dataTables_length,
    body.dark-mode .dataTables_wrapper .dataTables_filter,
    body.dark-mode .dataTables_wrapper .dataTables_info,
    body.dark-mode .dataTables_wrapper .dataTables_paginate {
        color: var(--text-primary);
    }

    body.dark-mode .dataTables_wrapper .dataTables_length select,
    body.dark-mode .dataTables_wrapper .dataTables_filter input {
        background-color: var(--card-bg);
        color: var(--text-primary);
        border-color: var(--border-color);
    }

    body.dark-mode .dataTables_wrapper .dataTables_paginate .paginate_button {
        color: var(--text-primary) !important;
    }

    body.dark-mode .dataTables_wrapper .dataTables_paginate .paginate_button:hover {
        color: var(--text-primary) !important;
        background: var(--table-hover);
        border-color: var(--border-color);
    }

    body.dark-mode .dataTables_wrapper .dataTables_paginate .paginate_button.current {
        background: var(--primary-gradient) !important;
        color: white !important;
        border-color: transparent !important;
    }

    /* Modal no modo escuro */
    body.dark-mode .modal-content {
        background-color: var(--card-bg);
        color: var(--text-primary);
    }

    body.dark-mode .modal-header {
        border-bottom-color: var(--border-color);
    }

    body.dark-mode .modal-footer {
        border-top-color: var(--border-color);
    }

    body.dark-mode .bg-light {
        background-color: var(--table-hover) !important;
        color: var(--text-primary);
    }

    body.dark-mode .close {
        color: white;
        opacity: 1;
    }

    /* Scroll customizado */
    .table-responsive::-webkit-scrollbar {
        height: 8px;
    }

    .table-responsive::-webkit-scrollbar-track {
        background: var(--border-color);
        border-radius: 10px;
    }

    .table-responsive::-webkit-scrollbar-thumb {
        background: var(--text-muted);
        border-radius: 10px;
    }

    .table-responsive::-webkit-scrollbar-thumb:hover {
        background: var(--text-secondary);
    }
</style>
{% endblock %}

{% block body %}
<div class="container-fluid px-3 px-lg-4 mt-4">
    
        <div class="page-header fade-in">
        <div class="d-flex justify-content-between align-items-start flex-wrap gap-3">
            <div>
                <h1><i class="fas fa-chart-line me-2"></i>Dashboard Completo</h1>
                <p class="subtitle mb-0">
                    Visão geral de todos os alertas e estatísticas do sistema
                </p>
            </div>
            <button class="btn btn-primary export-btn">
                <i class="fas fa-download me-2"></i>Exportar Dados
            </button>
        </div>
    </div>

        <div class="row mb-4">
        <div class="col-xl-3 col-md-6 mb-3 mb-xl-0 fade-in">
            <div class="stats-card primary">
                <div class="stats-card-icon">
                    <i class="fas fa-bell"></i>
                </div>
                <div class="stats-card-value">{{ activeAlertsToday }}</div>
                <div class="stats-card-label">Alertas Hoje</div>
            </div>
        </div>
        
        <div class="col-xl-3 col-md-6 mb-3 mb-xl-0 fade-in" style="animation-delay: 0.1s;">
            <div class="stats-card success">
                <div class="stats-card-icon">
                    <i class="fas fa-calendar-alt"></i>
                </div>
                <div class="stats-card-value">{{ totalAlertsThisMonth }}</div>
                <div class="stats-card-label">Alertas Mês</div>
            </div>
        </div>
        
        <div class="col-xl-3 col-md-6 mb-3 mb-md-0 fade-in" style="animation-delay: 0.2s;">
            <div class="stats-card info">
                <div class="stats-card-icon">
                    <i class="fas fa-road"></i>
                </div>
                <div class="stats-card-value">{{ traficdata.total_kms_lento }}</div>
                <div class="stats-card-label">KM Congestionados</div>
            </div>
        </div>
        
        <div class="col-xl-3 col-md-6 fade-in" style="animation-delay: 0.3s;">
            <div class="stats-card warning">
                <div class="stats-card-icon">
                    <i class="fas fa-clock"></i>
                </div>
                <div class="stats-card-value">{{ traficdata.total_atraso_minutos }}</div>
                <div class="stats-card-label">Atraso Total (min)</div>
            </div>
        </div>
    </div>

        <div class="row mb-4">
        
                <div class="col-xl-6 col-lg-12 mb-4 fade-in" style="animation-delay: 0.4s;">
            <div class="card modern-card">
                <div class="modern-card-header bg-danger-gradient">
                    <span>
                        <i class="fas fa-car-crash me-2"></i>
                        Acidentes Recentes
                    </span>
                    {% if accidentAlerts is not empty %}
                        <span class="badge-counter">{{ accidentAlerts|length }}</span>
                    {% endif %}
                </div>
                <div class="card-body p-0 position-relative">
                    {% if accidentAlerts is not empty %}
                        <div class="table-responsive">
                            <table class="table modern-table" id="accidentsTable">
                                <thead>
                                    <tr>
                                        <th>Cidade</th>
                                        <th class="d-none d-lg-table-cell">Rua</th>
                                        <th class="d-none d-md-table-cell">Data/Hora</th>
                                        <th class="text-center">Confiança</th>
                                        <th class="text-center">Ações</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    {% for alert in accidentAlerts %}
                                    <tr>
                                        <td>
                                            <div class="fw-bold text-danger">{{ alert.city }}</div>
                                            <small class="text-muted d-lg-none">{{ alert.street }}</small>
                                        </td>
                                        <td class="d-none d-lg-table-cell">
                                            <span class="text-truncate d-inline-block" style="max-width: 200px;">
                                                {{ alert.street }}
                                            </span>
                                        </td>
                                        <td class="d-none d-md-table-cell">
                                            <div class="text-nowrap">{{ (alert.pubMillis / 1000) | date("d/m/Y") }}</div>
                                            <small class="text-muted">{{ (alert.pubMillis / 1000) | date("H:i") }}</small>
                                        </td>
                                        <td class="text-center">
                                            <span class="modern-badge badge-{{ alert.confidence > 4 ? 'success' : (alert.confidence > 2 ? 'warning' : 'danger') }}">
                                                {{ alert.confidence }}
                                            </span>
                                        </td>
                                        <td class="text-center">
                                            <button class="btn btn-info action-btn open-modal" 
                                                    data-bs-toggle="modal"
                                                    data-bs-target="#alertModal"
                                                    data-alert="{{ {
                                                        'uuid': alert.uuid,
                                                        'city': alert.city,
                                                        'street': alert.street,
                                                        'location': [alert.location_x, alert.location_y],
                                                        'timestamp': alert.pubMillis,
                                                        'type': alert.type,
                                                        'subtype': alert.subtype,
                                                        'confidence': alert.confidence,
                                                        'status': alert.status
                                                    }|json_encode }}"
                                                    title="Ver Detalhes">
                                                <i class="fas fa-eye"></i>
                                            </button>
                                            <a href="https://www.waze.com/ul?ll={{ alert.location_y }},{{ alert.location_x }}"
                                               target="_blank" 
                                               class="btn btn-primary action-btn"
                                               title="Abrir no Waze">
                                                <i class="fab fa-waze"></i>
                                            </a>
                                        </td>
                                    </tr>
                                    {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    {% else %}
                        <div class="empty-state">
                            <i class="fas fa-road"></i>
                            <h5>Nenhum Acidente</h5>
                            <p>Não há acidentes reportados no momento.</p>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>

                <div class="col-xl-6 col-lg-12 mb-4 fade-in" style="animation-delay: 0.7s;">
            <div class="card modern-card">
                <div class="modern-card-header bg-info-gradient">
                    <span>
                        <i class="fas fa-exclamation-triangle me-2"></i>
                        Outros Alertas
                    </span>
                    {% if otherAlerts is not empty %}
                        <span class="badge-counter">{{ otherAlerts|length }}</span>
                    {% endif %}
                </div>
                <div class="card-body p-0 position-relative">
                    {% if otherAlerts is not empty %}
                        <div class="table-responsive">
                            <table class="table modern-table" id="othersTable">
                                <thead>
                                    <tr>
                                        <th>Cidade</th>
                                        <th class="d-none d-lg-table-cell">Rua</th>
                                        <th class="d-none d-md-table-cell">Data/Hora</th>
                                        <th class="text-center">Confiança</th>
                                        <th class="text-center">Ações</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    {% for alert in otherAlerts %}
                                    <tr>
                                        <td>
                                            <div class="fw-bold text-info">{{ alert.city }}</div>
                                            <small class="text-muted d-lg-none">{{ alert.street }}</small>
                                        </td>
                                        <td class="d-none d-lg-table-cell">
                                            <span class="text-truncate d-inline-block" style="max-width: 200px;">
                                                {{ alert.street }}
                                            </span>
                                        </td>
                                        <td class="d-none d-md-table-cell">
                                            <div class="text-nowrap">{{ (alert.pubMillis / 1000) | date("d/m/Y") }}</div>
                                            <small class="text-muted">{{ (alert.pubMillis / 1000) | date("H:i") }}</small>
                                        </td>
                                        <td class="text-center">
                                            <span class="modern-badge badge-{{ alert.confidence > 4 ? 'success' : (alert.confidence > 2 ? 'warning' : 'danger') }}">
                                                {{ alert.confidence }}
                                            </span>
                                        </td>
                                        <td class="text-center">
                                            <button class="btn btn-info action-btn open-modal" 
                                                    data-bs-toggle="modal"
                                                    data-bs-target="#alertModal"
                                                    data-alert="{{ {
                                                        'uuid': alert.uuid,
                                                        'city': alert.city,
                                                        'street': alert.street,
                                                        'location': [alert.location_x, alert.location_y],
                                                        'timestamp': alert.pubMillis,
                                                        'type': alert.type,
                                                        'subtype': alert.subtype,
                                                        'confidence': alert.confidence,
                                                        'status': alert.status
                                                    }|json_encode }}"
                                                    title="Ver Detalhes">
                                                <i class="fas fa-eye"></i>
                                            </button>
                                            <a href="https://www.waze.com/ul?ll={{ alert.location_y }},{{ alert.location_x }}"
                                               target="_blank" 
                                               class="btn btn-primary action-btn"
                                               title="Abrir no Waze">
                                                <i class="fab fa-waze"></i>
                                            </a>
                                        </td>
                                    </tr>
                                    {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    {% else %}
                        <div class="empty-state">
                            <i class="fas fa-exclamation-triangle"></i>
                            <h5>Nenhum alerta</h5>
                            <p>Não há outros alertas no momento.</p>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="alertModal" tabindex="-1" aria-labelledby="alertModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header bg-primary-gradient text-white">
                <h5 class="modal-title text-white">
                    <i class="fas fa-exclamation-triangle me-2"></i>Detalhes do Evento
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            
            <div class="modal-body" style="max-height: 75vh; overflow-y: auto;">
                <div class="row">
                    <div class="col-md-6 mb-3">
                        <div class="bg-light p-3 rounded border">
                            <h6 class="fw-bold mb-3">
                                <i class="fas fa-info-circle me-2"></i>Informações Básicas
                            </h6>
                            <p class="mb-2"><strong>UUID:</strong> <span id="modalUuid" class="text-muted"></span></p>
                            <p class="mb-2"><strong>Cidade:</strong> <span id="modalCity"></span></p>
                            <p class="mb-2"><strong>Rua:</strong> <span id="modalStreet"></span></p>
                            <p class="mb-0">
                                <strong>Localização:</strong><br>
                                Lat: <span id="modalLat"></span>, Lon: <span id="modalLon"></span>
                            </p>
                        </div>
                    </div>
                    
                    <div class="col-md-6 mb-3">
                        <div class="bg-light p-3 rounded border">
                            <h6 class="fw-bold mb-3">
                                <i class="fas fa-clock me-2"></i>Detalhes Adicionais
                            </h6>
                            <p class="mb-2"><strong>Data:</strong> <span id="modalDate"></span></p>
                            <p class="mb-2"><strong>KM:</strong> <span id="modalKm" class="badge bg-secondary"></span></p>
                            <p class="mb-2"><strong>Confiança:</strong> <span id="modalConfidence" class="badge"></span></p>
                            <p class="mb-2"><strong>Status:</strong> <span id="modalStatus" class="badge"></span></p>
                            <p class="mb-2"><strong>Tipo:</strong> <span id="modalType"></span></p>
                            <p class="mb-0"><strong>Subtipo:</strong> <span id="modalSubtype"></span></p>
                        </div>
                    </div>
                </div>
                
                <hr class="my-3">
                
                <h6 class="text-center text-muted mb-3">
                    <i class="fas fa-map-marked-alt me-2"></i>Localização no Mapa (Waze Embed)
                </h6>
                <iframe id="modalMap" class="rounded border w-100" style="height: 300px;" loading="lazy"></iframe>
                
                <hr class="my-3">
                
                <h6 class="text-center text-muted mb-3">
                    <i class="fas fa-street-view me-2"></i>Street View
                </h6>
                <div class="alert alert-info text-center" role="alert">
                    O Street View não pode ser embutido diretamente devido a restrições de API. Use o link na seção de Ações para visualizar.
                </div>
            </div>
            
            <div class="modal-footer">
                <a id="modalStreetViewLink" class="btn btn-outline-secondary me-auto" target="_blank" title="Abrir Google Street View">
                    <i class="fas fa-external-link-alt me-2"></i> Abrir Street View
                </a>
                <a id="modalWazeLink" class="btn btn-primary" target="_blank">
                    <i class="fab fa-waze me-2"></i>Ver no Waze
                </a>
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                    <i class="fas fa-times me-2"></i>Fechar
                </button>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
<script src="https://cdn.datatables.net/v/bs5/dt-1.13.10/r-2.5.0/datatables.min.js"></script>
<script>

// ===========================================
// FUNÇÃO PLACEHOLDER ASYNC PARA BUSCA DE KM
// ATENÇÃO: Substitua esta função pela sua chamada de API real!
// ===========================================
async function buscarKmDnit(latitude, longitude) {
    console.log(`Simulando busca KM para Lat: ${latitude}, Lon: ${longitude}`);
    // Simula um atraso de rede
    await new Promise(resolve => setTimeout(resolve, 800));
    
    // Simula o resultado (remova esta lógica ao implementar o backend)
    const randomValue = Math.random();
    
    if (randomValue < 0.2) {
         // Simula KM não encontrado
         return null; 
    } else if (randomValue < 0.4) {
         // Simula um erro de API
         throw new Error('Erro 500 ao consultar a API de KM');
    }
    // Retorna um KM simulado entre 100 e 600
    return (Math.random() * 500) + 100; 
}


document.addEventListener("DOMContentLoaded", function () {
    
    // Inicializar Tooltips
    const tooltipTriggerList = [].slice.call(document.querySelectorAll('[title]'));
    tooltipTriggerList.map(function (tooltipTriggerEl) {
        // Usamos o Bootstrap 5 nativo (data-bs-toggle/data-bs-target)
        return new bootstrap.Tooltip(tooltipTriggerEl); 
    });

    // Configuração do DataTable
    const dataTableConfig = {
        language: {
            url: '//cdn.datatables.net/plug-ins/1.10.25/i18n/Portuguese-Brasil.json'
        },
        scrollX: true,
        responsive: true,
        pageLength: 10,
        lengthMenu: [[10, 25, 50, -1], [10, 25, 50, "Todos"]],
        order: [[2, 'desc']],
        columnDefs: [
            { orderable: false, targets: [4] }
        ]
    };

    // Inicializar DataTables (Apenas as tabelas presentes no HTML)
    if ($.fn.DataTable) {
        if ($('#accidentsTable').length) {
            $('#accidentsTable').DataTable(dataTableConfig);
        }
        if ($('#othersTable').length) {
            $('#othersTable').DataTable(dataTableConfig);
        }
    }

    // Configuração do Modal (Lógica ASYNC COMPLETA)
    const alertModal = document.getElementById('alertModal');
    if (alertModal) {
        // Usamos uma função assíncrona para suportar a busca de KM
        alertModal.addEventListener('show.bs.modal', async function (event) {
            const button = event.relatedTarget;
            
            // Resetar KM
            const kmSpan = document.getElementById('modalKm');
            kmSpan.textContent = '';
            kmSpan.className = 'badge bg-secondary';

            try {
                const rawData = button.getAttribute('data-alert');
                const alertData = JSON.parse(rawData);
                
                console.log('Dados do alerta:', alertData);
                
                const [lon, lat] = alertData.location; // Waze usa [lon, lat]

                // 1. Preencher dados básicos
                document.getElementById('modalUuid').textContent = alertData.uuid;
                document.getElementById('modalCity').textContent = alertData.city;
                document.getElementById('modalStreet').textContent = alertData.street;
                document.getElementById('modalLat').textContent = lat.toFixed(6);
                document.getElementById('modalLon').textContent = lon.toFixed(6);
                document.getElementById('modalDate').textContent = new Date(alertData.timestamp).toLocaleString('pt-BR');
                document.getElementById('modalType').textContent = alertData.type;
                document.getElementById('modalSubtype').textContent = alertData.subtype || 'N/A';
                
                // 2. Atualizar Status
                const statusBadge = document.getElementById('modalStatus');
                statusBadge.textContent = alertData.status === 1 ? "Ativo" : "Inativo";
                statusBadge.className = `badge bg-${alertData.status === 1 ? 'success' : 'danger'}`;
                
                // 3. Atualizar Confiança
                const confidenceBadge = document.getElementById('modalConfidence');
                const confidence = alertData.confidence;
                confidenceBadge.textContent = confidence;
                // Lógica do badge de confiança (igual à tabela)
                let confidenceClass = confidence > 4 ? 'success' : (confidence > 2 ? 'warning' : 'danger');
                confidenceBadge.className = `badge bg-${confidenceClass}`;

                // 4. Atualizar Iframes e Links
                const wazeMapSrc = `https://embed.waze.com/iframe?zoom=16&lat=${lat}&lon=${lon}&pin=1`;
                document.getElementById('modalMap').src = wazeMapSrc;
                
                const wazeLink = `https://www.waze.com/ul?ll=${lat},${lon}`;
                document.getElementById('modalWazeLink').href = wazeLink;
                
                // Link externo para Google Street View
                const streetViewLink = `https://www.google.com/maps/@?api=1&map_action=pano&viewpoint=${lat},${lon}`;
                document.getElementById('modalStreetViewLink').href = streetViewLink;
                
                // 5. Lógica de Busca de KM (Assíncrona)
                kmSpan.innerHTML = '<span class="spinner-border spinner-border-sm"></span> Calculando...';
                kmSpan.className = 'badge bg-secondary';
                
                try {
                    const km = await buscarKmDnit(lat, lon);
                    
                    if (km !== null && !isNaN(parseFloat(km))) {
                        kmSpan.textContent = `KM ${parseFloat(km).toFixed(2).replace('.', ',')}`;
                        kmSpan.className = 'badge bg-info';
                    } else {
                        kmSpan.textContent = 'Não Encontrado';
                        kmSpan.className = 'badge bg-warning';
                    }
                } catch (error) {
                    console.error("Erro ao buscar KM:", error);
                    kmSpan.textContent = 'Erro na Busca';
                    kmSpan.className = 'badge bg-danger';
                }
                
            } catch (error) {
                console.error('Erro ao carregar dados do alerta:', error);
                // Em caso de erro grave, fecha o modal e notifica
                const modalInstance = bootstrap.Modal.getInstance(alertModal);
                if (modalInstance) {
                    modalInstance.hide();
                }
                alert('Erro ao carregar detalhes do alerta. Verifique o console.');
            }
        });
    }

});
</script>
{% endblock %}