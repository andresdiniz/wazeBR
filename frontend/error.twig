<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Erro - {{ titulo|default('Erro Desconhecido') }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            background-color: #f8d7da;
            color: #721c24;
            padding: 50px;
        }
        .error-container {
            display: inline-block;
            padding: 30px;
            background-color: #fff;
            border: 1px solid #f5c6cb;
            border-radius: 8px;
            max-width: 600px;
        }
        .error-container h1 {
            font-size: 50px;
            margin-bottom: 10px;
        }
        .error-container h2 {
            font-size: 30px;
            margin-bottom: 20px;
        }
        .error-container p {
            font-size: 18px;
            margin: 10px 0;
        }
        .error-container a {
            color: #721c24;
            text-decoration: none;
            font-weight: bold;
        }
        .error-container a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>

    <div class="error-container">
        <h1>Erro {{ errorCode|default('000') }}</h1>
        <h2>{{ errorTitle|default('Erro inesperado') }}</h2>
        <p>{{ errorMessage|default('Algo deu errado ao processar sua requisição.') }}</p>
        {% if errorDescription is defined %}
            <p><em>{{ errorDescription }}</em></p>
        {% endif %}
        <p><a href="{{ pagina_retorno|default('/') }}">Voltar para a página inicial</a></p>
    </div>

</body>
</html>
